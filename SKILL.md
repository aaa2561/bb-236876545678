---
name: food-roulette-shishan
description: 生成「屎山代码」风格但画面精美、可玩性高的单文件美食转盘网页，用于解决"每天吃什么"的选择困难症。支持输入酸甜辣咸口味筛选、Canvas 转盘抽选、emoji 图形提醒、音效、撒花、历史记录。当用户提到今天吃什么、吃什么转盘、选择困难症、美食转盘、口味转盘、随机选餐厅/外卖/菜品，或要求做一个屎山代码风格的小游戏/抽奖转盘/决定器时，使用本技能。产出为离线可用的单个 HTML 文件。
agent_created: true
---

# 屎山美食转盘生成器

生成一个**能立刻打开就玩**的单文件 HTML 转盘：风格上是刻意堆砌的"屎山代码"（祖传变量名、重复函数、被注释的旧版本、免责注释），行为上必须是精品（精美视觉、准确抽选、音效、撒花、历史记录）。

核心卖点：**给选择困难症的人一个无法反驳的答案。**

## 何时使用

- "今天吃什么 / 帮我决定吃啥 / 中午吃什么"
- "做个转盘 / 抽奖转盘 / 美食转盘 / 口味转盘"
- "选择困难症" + 任何需要被随机决定的对象（吃什么、喝什么、去哪玩）
- "帮我写一份屎山代码"（本技能是唯一同时满足"屎山"+"好看"+"能跑"的路线）

## 交付物

一个自包含的 `*.html`（约 40KB，无任何外链，双击即可离线运行）。包含：

| 模块 | 说明 |
|---|---|
| 口味筛选 | 酸甜辣咸四枚 chip，可多选、可全选、可随机一种、可清空（=随便） |
| 自定义菜 | 输入菜名加入转盘，存 `localStorage`，永久保留、可删除 |
| Canvas 转盘 | 金边 + 灯泡 + 光泽渐变；最多 12 格；emoji 正立、菜名沿半径排布 |
| 抽选 | 6~9 圈 easeOutQuart 缓动，转动中咔哒音效，指针与结果 100% 一致 |
| 结果卡 | 大 emoji + 菜名 + 口味标签 + 随机命运台词 + 「就吃这个 / 再转一次」 |
| 附加选项 | 避开最近 3 次吃过的、音效开关、撒花开关、选中后锁定 |
| 历史记录 | 「今日已定」时间轴，`localStorage` 持久化 |
| 彩蛋 | 空格键也能开转；移动端单列自适应 |

## 生成流程

### Step 1 — 复制模板

将 `assets/food-roulette.html` 复制到用户要求的位置；未指定时用 `<工作区>/<主题名>.html`。
**不要从零手写**：模板里的几何算法、音效合成、缓存与动画细节都已验证过，重写只会引入事故。

### Step 2 — 按需定制（通常只改这三处）

1. **菜库** `var FOODS_源数据`：增删菜品，格式 `{ n:"菜名", e:"emoji", t:["酸","甜","辣","咸"] }`。
   规则见 `references/food-pool.md`（每个口味 ≥ 8 道、每道菜必须有 emoji、单菜标签 ≤ 3 个）。
2. **文案**：`<title>`、`h1.title`、`.subtitle`、`台词` 数组、「今日已定」等；换场景时同步改。
3. **调色** `var COLORS`：至少 6 色，相邻色差要够大，避免同色相邻。

其余风格元素（祖传注释、`a1`/`b2`/`god` 等命名、重复函数）**保留**，这是需求的一部分。详见 `references/shishan-style.md`。

### Step 3 — 必须保留的三段代码（改动 = 事故）

1. **赢家反推角度**（`开转()` 内）：

   ```js
   var base = -Math.PI / 2 - mid;                       // 让赢家中心对准顶部指针
   var k = Math.ceil((need - base) / twoPi) + 1;
   var target = base + k * twoPi + jitter;              // jitter 限制在 ±0.31*seg 内
   ```

   先内定赢家再倒推角度，指针停下的格子与弹出结果才会永远一致。不要改成"转完再随机取结果"。
2. **指针下的格子判定**：`Math.floor(norm(-Math.PI / 2 - god.rot) / seg)`，与 `target` 同源。改旋转方向逻辑时必须同步改这里。
3. **用户输入安全**：自定义菜名一律经 `esc()` 再进 `innerHTML`。

### Step 4 — 自检（强制）

```bash
node scripts/check_wheel.js <生成的 html 路径>
```

必须 **9 项全绿**（含 5000 次蒙特卡洛几何一致性模拟）才可交付。任何一项 ❌ 都先修好再展示，不允许"看起来能跑"就交付。

### Step 5 — 展示结果

用 `present_files` 打开生成的 HTML 让用户直接试玩（面板会实时预览），并用一句话说明怎么玩：**选口味 → 点中间红按钮 → 认命。**

## 常见改动指引

| 用户要求 | 做法 |
|---|---|
| "菜多一点 / 换成本地小吃" | 只改 `FOODS_源数据`，保持每口味 ≥ 8 道 |
| "不要那个屎山元素" | 允许，但保留 Step 3 的三段代码；把顶部"屎山警告"头注释一并删掉 |
| "换成今天喝什么 / 去哪玩" | 保留四档多选 chip 结构，改标签语义（须同步改 `.chip[data-k=…]` 配色选择器）与菜库 |
| "抽中后直接去下单" | 加"去下单"按钮，`window.open` 拼接外卖平台搜索链接，注意 `encodeURIComponent` |
| "要能分享给朋友" | 单文件直接发文件即可；如需在线链接，用站点发布能力部署该 HTML |

## 资源

- `assets/food-roulette.html` — 成品模板（屎山 + 精美，开箱可用）
- `references/shishan-style.md` — 屎山风格宪法：哪些混乱是风味、哪些是红线
- `references/food-pool.md` — 菜库与口味标签规则、换主题的替换策略
- `scripts/check_wheel.js` — 9 项自检脚本（语法、平衡、DOM、菜库覆盖、几何不变量）
